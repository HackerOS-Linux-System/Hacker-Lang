// golang
// odin
// cargo
// pyhon3.13

> cd source-code
> cd core
>>> cd hl-compiler && cargo build --release && cd .. && cd hl-runtime && cargo build --release && cd .. && cd hl-plsa && cargo build --release && cd .. && cd hl-containers && go get hl-containers && go build

> cd ..
> cd cli
>>> cd hli && cargo build --release && cd .. && cd hl && odin build . && cd .. && cd hlh && odin build . && cd .. && cd hla && odin build .
